# nazo-gui プロジェクト固有ルール

グローバルのCLAUDE.mdにあるt_wada式TDDの強制は、**このプロジェクトでは適用しない**（本人の希望による例外）。

## テスト方針

- 見た目・演出系のコンポーネント（Canvas描画、UIの見た目、アニメーションなど）にはテストを書かない。実際にブラウザで確認する。
- `src/utils/`配下のような純粋なロジック（座標生成、当たり判定など副作用のない関数）には軽くテストを書く。
- 開発フローはTDD厳格運用ではなく、実装してから必要に応じてロジック部分のテストを足す形でよい。
